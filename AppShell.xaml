<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:CinemaReservationApp" 
       x:Class="CinemaReservationApp.AppShell">
    <FlyoutItem Title="Home" Icon="home.jpg">
        <ShellContent Title="Movies" ContentTemplate="{DataTemplate local:HomePage}" />
    </FlyoutItem>
    <FlyoutItem Title="My Reservations" Icon="reservations.jpeg">
        <ShellContent Title="Reservations" ContentTemplate="{DataTemplate local:MyReservationsPage}" />
    </FlyoutItem>
</Shell>
